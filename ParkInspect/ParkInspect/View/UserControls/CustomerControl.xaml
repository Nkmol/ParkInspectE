<UserControl x:Class="ParkInspect.View.UserControls.CustomerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             Width="1000"
             Height="600"
             DataContext="{Binding Client, Source={StaticResource Locator}}">
    <Grid>
        <TabControl>
            <TabItem Header="Overzicht">
                <DataGrid 
                    ItemsSource="{Binding Clients}" 
                    SelectedItem="{Binding SelectedClient}"
                    ScrollViewer.CanContentScroll="True" VerticalAlignment="Top"
                    ScrollViewer.VerticalScrollBarVisibility="Visible"
                    ScrollViewer.HorizontalScrollBarVisibility="Visible" 
                    SelectionMode="Single" AutoGenerateColumns="False" 
                    IsReadOnly="True" MaxHeight="500">

                    <DataGrid.Columns>
                        <DataGridTextColumn Width="150" Header="Naam" Binding="{Binding name}"/>
                        <DataGridTextColumn Width="150" Header="Telefoonnummer" Binding="{Binding phonenumber}"/>
                        <DataGridTextColumn Width="150" Header="Emailadres" Binding="{Binding email}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Management" DataContext="{Binding SelectedClient}">
                <Grid>
                    <DockPanel Width="480" Height="250" VerticalAlignment="Top" HorizontalAlignment="Left">
                        <StackPanel>
                            <Label x:Name="NameLabel" Content="Naam Bedrijf"/>
                            <TextBox x:Name="Name" Text="{Binding name}" Margin="0,0,0,10" Width="250" HorizontalAlignment="Left"/>

                            <Label x:Name="TelnrLabel" Content="Telefoonnummer"/>
                            <TextBox x:Name="Telnr" Text="{Binding phonenumber}" HorizontalAlignment="Left" Margin="0,0,0,10" Width="250"/>

                            <Label x:Name="EmailLabel" Content="Emailadres"/>
                            <TextBox x:Name="Email" Text="{Binding email}" HorizontalAlignment="Left" Margin="0,0,0,10" Width="250"/>
                        </StackPanel>
                    </DockPanel>
                    <DockPanel Width="480" Height="250" VerticalAlignment="Top" HorizontalAlignment="Right">
                        <StackPanel>
                            <Label>Overige informatie ofso?</Label>
                            <Label>Geselecteerde klant zijn contactpersoon info tonen?</Label>
                        </StackPanel>
                    </DockPanel>
                    <StackPanel DataContext="{Binding Client, Source={StaticResource Locator}}" Margin="0,250,0,0">
                        <Button HorizontalContentAlignment="Left" Command="{Binding CompleteClientCommand}" Content="Voeg toe" Margin="0,0,0,10" HorizontalAlignment="Left"/>
                        <Button HorizontalContentAlignment="Left" Command="{Binding UpdateButtonCommand}" Content="Bewerken" Margin="0,0,0,10" HorizontalAlignment="Left"/>
                        <Button HorizontalContentAlignment="Left" Command="{Binding ResetButtonCommand}" Content="Deselecteren" Margin="0,0,0,10" HorizontalAlignment="Left"/>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>