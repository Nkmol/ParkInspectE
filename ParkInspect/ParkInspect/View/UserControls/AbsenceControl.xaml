<UserControl x:Class="ParkInspect.View.UserControls.AbsenceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ParkInspect.View.UserControls"
             xmlns:convertor="clr-namespace:ParkInspect.Convertor"
             mc:Ignorable="d" 
             Height="Auto"
             Width="Auto"
             DataContext="{Binding Absence, Source={StaticResource Locator}}">
    <UserControl.Resources>
        <convertor:VisibilityConverter x:Key="VisibilityConverter"/>
    </UserControl.Resources>


    <Grid>
        <TabControl Margin="0,0,-0.2,0">
            <TabItem Header="Overview" Visibility="{Binding Dashboard.TabStatus[10], Converter={StaticResource VisibilityConverter}, Mode=TwoWay, Source= {StaticResource Locator}}">
                <ScrollViewer>
                    <StackPanel>
                        <DataGrid SelectedItem="{Binding SelectedAbsence}" ItemsSource="{Binding Absences}" IsReadOnly="True"  AutoGenerateColumns="False" VerticalAlignment="Top" HorizontalAlignment="Left">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Start Datum" Binding="{Binding start}"  />
                                <DataGridTextColumn Header="Eind Datum" Binding="{Binding end}" />
                                <DataGridTextColumn Header="Voornaam" Binding="{Binding Employee.firstname}"/>
                                <DataGridTextColumn Header="Achternaam" Binding="{Binding Employee.lastname}"/>
                                <DataGridTextColumn Header="ID" Binding="{Binding employee_id}"/>

                            </DataGrid.Columns>
                        </DataGrid>
                        <Button Command="{Binding DeleteAbsenceCommand}" x:Name="button1" Content="Verwijder" HorizontalAlignment="Left" Margin="0,10,0,0" Width="120"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem  Header="Afwezigheid invoeren">
                <Grid>
                    <DockPanel Height="150" Width="255" Margin="100,0,0,0" VerticalAlignment="Top" HorizontalAlignment="Left">
                        <StackPanel>
                            <Label x:Name="label" Content="Begin Datum"/>
                            <DatePicker SelectedDate="{Binding NewAbsence.start}" Width="250" HorizontalAlignment="Left"  Margin="5,0,0,10" />

                            <Label x:Name="label3" Content="Medewerker"/>
                            <ComboBox Margin="5,0,0,10" ItemsSource="{Binding Employees}" SelectedValue="{Binding SelectedEmployee}" DisplayMemberPath="firstname" Text="Selecteer Personeel"></ComboBox>
                        </StackPanel>
                    </DockPanel>
                    <DockPanel Height="150" Width="255" Margin="0,0,100,0" VerticalAlignment="Top" HorizontalAlignment="Right">
                    <StackPanel>
                            <Label x:Name="label2" Content="Eind Datum"/>
                            <DatePicker  SelectedDate="{Binding NewAbsence.end}" Width="250" HorizontalAlignment="Left"  Margin="5,0,0,10"/>
                        </StackPanel>
                        </DockPanel>
                    <StackPanel Orientation="Horizontal" Margin="100,320,100,0">
                        <Label DataContext="{Binding Absence, Source={StaticResource Locator}}" Content="{Binding Notification, Mode=TwoWay}" Width="300" Height="50" VerticalAlignment="Top"/>
                        <Button Command="{Binding SaveNewAbsenceCommand}" x:Name="button"  Content="Opslaan" HorizontalAlignment="Left" Margin="240,0,10,0" VerticalAlignment="Top" Width="120"/>
                        <Button VerticalAlignment="Top" Margin="0" Width="120" Content="Deselecteren"></Button>
                    </StackPanel>
                </Grid>
            </TabItem>


        </TabControl>
    </Grid>
</UserControl>
