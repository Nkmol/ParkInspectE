<controls:MetroWindow x:Class="ParkInspect.ReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"            
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:local="using:ParkInspect.View.UserControls"
        xmlns:userControls="clr-namespace:ParkInspect.View.UserControls"
        xmlns:popup="clr-namespace:ParkInspect.View.UserControls.Popup"
        xmlns:dragablz="http://dragablz.net/winfx/xaml/dragablz"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
        xmlns:viewModel="clr-namespace:ParkInspect.ViewModel"
        mc:Ignorable="d ignore"
        Height="600"
        Width="1010"
        Title="ParkInspect"
        GlowBrush="{DynamicResource AccentColorBrush}"
        BorderThickness="1"
        DataContext="{Binding Reports, Source={StaticResource Locator}}"
        popup:PopupParticipation.Register="{Binding PopupManager, Source={StaticResource Locator}}">
    <Grid>
        <!-- <syncfusion:ReportViewer x:Name="Viewer" ReportPath="{Binding Report.ReportPath}"/> -->
        <syncfusion:ReportViewer x:Name="Viewer" ReportPath="{Binding Report.ReportPath}"/>
    </Grid>
</controls:MetroWindow>
